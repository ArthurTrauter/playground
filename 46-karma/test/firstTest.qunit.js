(function ($) {
	"use strict";

	QUnit.module("CombinationLock API", {
		beforeEach : function () {
		},
		afterEach : function () {
		}
	});

	QUnit.test( "hello test", function( assert ) {
		assert.ok( 1 == "1", "Passed!" );
		assert.notOk(false, "Passed again");
	});

})();